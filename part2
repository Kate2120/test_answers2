 1. Какими способами можно подгрузить js в html браузере? и чем они отличаются друг от друга?
 можно подключить через тег <script></script> в котором будет src на файл с кодом, например <script type="text/javascript" src="main.js"></script>
 второй вариант написать когд прямо в html в теге <script></script> ,например 
 <script>
function f(n, sum) {
    sum = sum || 0;
    for(let i = 0; i < n; i++) {
        sum += i;
    }
    return sum;
}
f();
 </script>
 
 
VM115:109 2. Какие существуют типы данных (Какие из них примитивы, какие ссылочные)?
есть примитивные типы данных -  это когда в переменой находится значение. Есть ссылочные типы данных - это когда в переменной записана ссылка. При копировании переменных - примитивное значение копируется и в новой переменной если мы его изменим, то тозначение с которого мы скопировали не изменитя. А если мы скопируем сслыочный тип данных - то при измемнии изменится исходный код, т.к. мы скопировали сслыку и обект или функция, на которую ведет ссылка становятся доступны по 2 сслыкам
К примитивынм данным относится string, number, boolean, bigint, unndefined, null, symbol
